# Copy this file to .env.local and fill in your Supabase project's values
# (Project Settings → API in the Supabase dashboard). See docs/10-authentication.md.
# .env.local is gitignored — never commit real keys.
#
# VITE_SUPABASE_PUBLISHABLE_KEY is the "Publishable key" shown in the
# Supabase dashboard (safe to expose in frontend code) — never the
# "Secret key" / service-role key, which must never reach the browser.

VITE_SUPABASE_URL=https://vhaixlbzmxbosxlepmsk.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_-w0Tplq1_SgU53rZtWfMQQ_yljhae_P
